//Function name calculate area//
function calculateArea() {
    //Declaring radius as 7//
var Radius = 7;
    //formula to calculate the area of circle//
var result = (Radius * Radius* Math.PI );
    //displaying in console//
console.log(result);
}